% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{cssem_mediation}
\alias{cssem_mediation}
\title{Decompose an associational mediation effect on locked construct states}
\usage{
cssem_mediation(
  association,
  x,
  y,
  mediators = NULL,
  disattenuate = TRUE,
  eiv_bootstrap = 0L,
  delta = 1,
  seed = 1L
)
}
\arguments{
\item{association}{A \code{cssem_association} from \code{\link[=cssem_associate]{cssem_associate()}}.}

\item{x}{Name of the locked predictor construct.}

\item{y}{Name of the locked outcome construct.}

\item{mediators}{Optional character vector restricting the displayed
path-specific effects to paths whose mediators all lie in this set. Total,
direct, and indirect effects always reflect the full declared structure.}

\item{disattenuate}{Whether to report errors-in-variables disattenuated
effects when the association carries reliability. Defaults to \code{TRUE}.}

\item{eiv_bootstrap}{Number of percentile-bootstrap resamples for the
intervals. Zero (default) omits intervals.}

\item{delta}{Size of the \code{x} contrast, in standard-deviation units of the
standardized construct states. Defaults to one. Effects are reported per
\code{delta}; for linear models they are scale-invariant.}

\item{seed}{Bootstrap seed.}
}
\value{
An object of class \code{cssem_mediation}.
}
\description{
Decomposes the effect of \code{x} on \code{y} into total, direct, and indirect
components, plus one path-specific indirect effect for every directed path
from \code{x} to \code{y} declared in the association's structure. Single, parallel, and
serial mediation are handled uniformly: effects are obtained by simulating an
\code{x} shift and propagating it through the fitted construct-level effect models,
so the decomposition is correct for nonlinear edges, not only linear paths.
}
\details{
Linear and monotone edges are disattenuated with the same errors-in-variables
correction used by \code{\link[=cssem_associate]{cssem_associate()}}, recovering the latent-scale indirect
effect that measurement error attenuates (the bias is largest for indirect
effects, which compound error across paths). Any path through a smooth edge is
reported without disattenuation. The decomposition is associational: it
requires the structure's declared temporal order and makes no causal claim.
}
\examples{
# association <- cssem_associate(fit, structure)
# cssem_mediation(association, x = "Trust", y = "Loyalty", eiv_bootstrap = 200)
}
