% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{run_measurement_benchmark}
\alias{run_measurement_benchmark}
\title{Run a small CS-SEM versus CB-SEM/PLS scoring benchmark}
\usage{
run_measurement_benchmark(
  reps = 20L,
  n = 400L,
  seed = 1L,
  tolerance = 0.02,
  design = NULL,
  folds = 3L,
  iterations = 4L
)
}
\arguments{
\item{reps}{Number of independent replications per scenario.}

\item{n}{Respondent count for the default single scenario; ignored when
\code{design} is supplied.}

\item{seed}{Integer seed used to derive deterministic replication seeds.}

\item{tolerance}{Non-inferiority tolerance used for the \code{success} attribute.}

\item{design}{Optional scenario data frame with columns \code{n}, \code{loading},
\code{missing}, \code{local_dependence}, and \code{cross_loading}.}

\item{folds}{Cross-fitting folds used inside each benchmark fit.}

\item{iterations}{Measurement iterations used inside each benchmark fit.}
}
\value{
A data frame of scenario results. Its \code{success_criterion} and
\code{success} attributes describe the predeclared screening gate.
}
\description{
The CB-SEM comparator is a deterministic ordinal-CFA score proxy: a first
factor of standardized ordinal indicators. Install and use lavaan in a
confirmatory study for a full DWLS CFA fit; this dependency-light benchmark
is intentionally runnable in a clean R installation.
}
\examples{
benchmark <- run_measurement_benchmark(
  design = cssem_validation_design("screening"), reps = 1, seed = 1
)
attr(benchmark, "success")
}
