% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-mediation.R
\name{causal_indirect_effect}
\alias{causal_indirect_effect}
\title{Estimate an interventional (causal) mediation effect on locked construct states}
\usage{
causal_indirect_effect(
  association,
  x,
  y,
  adjust,
  mediators = NULL,
  temporal_order = NULL,
  estimand = c("interventional"),
  disattenuate = TRUE,
  eiv_bootstrap = 0L,
  delta = 1,
  seed = 1L,
  design = NULL
)
}
\arguments{
\item{association}{A \code{cssem_association} from \code{\link[=associate]{associate()}}.}

\item{x}{Name of the locked treatment construct.}

\item{y}{Name of the locked outcome construct.}

\item{adjust}{Character vector of adjustment-set (confounder) construct names.
Must be declared predictors of the outcome and of each mediator.}

\item{mediators}{Optional character vector of mediator constructs. Defaults to
every intermediate construct on a declared directed path from \code{x} to \code{y}.}

\item{temporal_order}{Optional character vector asserting the temporal order of
the constructs; required for a \code{causal_under_assumptions} label.}

\item{estimand}{Causal estimand. Currently \code{"interventional"}.}

\item{disattenuate}{Whether to disattenuate using the association reliability.
Defaults to \code{TRUE}.}

\item{eiv_bootstrap}{Number of percentile-bootstrap resamples for the
intervals. Zero (default) omits intervals.}

\item{delta}{Size of the \code{x} contrast in standardized units. Defaults to one.}

\item{seed}{Bootstrap seed.}

\item{design}{Optional \code{\link{causal_design}()} for graph and mediation-assumption
auditing. When supplied, a causal label requires an admissible backdoor
design, all required assumptions, and causal-DAG edges for the analyzed
treatment-to-outcome paths.}
}
\value{
An object of class \code{causal_indirect_effect}.
}
\description{
Elevates the disattenuated mediation decomposition of \code{\link[=indirect_effect]{indirect_effect()}} to a
causal estimand. The effect of \code{x} on \code{y} is decomposed into interventional
total, direct, and indirect effects by simulating an \code{x} shift and propagating
it through the disattenuated construct-level models while holding the declared
\code{adjust} set fixed, so the indirect effect is adjusted for the confounders you
declare. For additive mediator models this g-computed indirect effect equals
the interventional indirect effect.
}
\details{
Unlike \code{\link[=indirect_effect]{indirect_effect()}}, this estimator enforces causal discipline: it
requires a non-empty adjustment set, requires those constructs to be declared
predictors of the outcome and of each mediator (so every stage model conditions
on them), refuses to adjust for any construct that is downstream of the
treatment (a post-treatment adjustment would bias the effect), and reports a
causal-admissibility panel. A \code{causal_under_assumptions} label additionally
requires a declared \code{temporal_order}; otherwise the effect is reported as an
adjusted association. Exposure-induced mediator-outcome confounding and
mediator interactions are not yet modeled.
}
\examples{
# causal_indirect_effect(association, x = "Satisfaction", y = "Loyalty",
#   adjust = "Trust", temporal_order = c("Trust", "Satisfaction", "Commitment", "Loyalty"))
}
