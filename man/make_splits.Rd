% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splits.R
\name{make_splits}
\alias{make_splits}
\title{Create reusable measurement and outer-validation splits}
\usage{
make_splits(
  data,
  method = c("random", "group", "time"),
  folds = 5L,
  group = NULL,
  time = NULL,
  seed = 1L
)
}
\arguments{
\item{data}{A data frame whose rows receive split IDs.}

\item{method}{Split design: \code{"random"}, \code{"group"}, or \code{"time"}.}

\item{folds}{Number of fold intervals. Time designs use the first interval
only for training and return outer partitions for later intervals.}

\item{group}{A column name or vector identifying repeated entities. Repeated
labels are allowed but one label is never split across folds.}

\item{time}{A column name or vector defining temporal order. Equal values
remain in the same time block.}

\item{seed}{Seed for random and group assignments.}
}
\value{
A \code{cssem_splits} object with an integer \code{assignment}, reusable outer
train/test row IDs, provenance, and independent-unit counts. Grouped
assignments retain source labels so leakage can be checked before fitting.
}
\description{
Creates reproducible split assignments for cross-fitting and outer evaluation.
Integer row IDs are authoritative; row names are retained only as labels.
}
\examples{
\dontrun{
splits <- make_splits(data.frame(x = rnorm(40)), folds = 4, seed = 2)
print(splits)
}
}
