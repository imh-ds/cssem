% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moderation.R
\name{cssem_moderated_mediation}
\alias{cssem_moderated_mediation}
\title{Decompose a moderated mediation effect on locked construct states}
\usage{
cssem_moderated_mediation(
  association,
  x,
  y,
  moderator,
  levels = c(-1, 0, 1),
  disattenuate = TRUE,
  eiv_bootstrap = 0L,
  delta = 1,
  seed = 1L
)
}
\arguments{
\item{association}{A \code{cssem_association} from \code{\link[=cssem_associate]{cssem_associate()}} whose
structure declares the mediating paths and the moderating interaction.}

\item{x}{Predictor construct name.}

\item{y}{Outcome construct name.}

\item{moderator}{Moderator construct name; must occur in an interaction term.}

\item{levels}{Moderator levels, in standard-deviation units of the
standardized construct states. Defaults to low, mean, and high (-1, 0, 1).}

\item{disattenuate}{Report disattenuated effects when reliability is
available. Defaults to \code{TRUE}.}

\item{eiv_bootstrap}{Percentile-bootstrap resamples for intervals. Zero omits.}

\item{delta}{Predictor contrast in standard-deviation units. Defaults to one.}

\item{seed}{Bootstrap seed.}
}
\value{
An object of class \code{cssem_moderated_mediation}.
}
\description{
Estimates the indirect effect of \code{x} on \code{y} through the declared mediators as
a function of a \code{moderator} construct: the conditional indirect effect at each
moderator level and the index of moderated mediation (its change per
standard-deviation unit of the moderator). The moderator must appear in a
declared interaction term (colon syntax, e.g. \code{"M:W"}) in the association's
structure. Linear and monotone edges are disattenuated as in
\code{\link[=cssem_mediation]{cssem_mediation()}}; the decomposition is associational and makes no causal
claim.
}
\examples{
# cssem_moderated_mediation(association, "X", "Y", "W", eiv_bootstrap = 500)
}
