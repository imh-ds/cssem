% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_cssem_data}
\alias{simulate_cssem_data}
\title{Simulate manifestation-construct data for deterministic validation}
\usage{
simulate_cssem_data(
  n = 400L,
  items = 4L,
  loading = 0.8,
  missing = 0.05,
  local_dependence = 0,
  cross_loading = 0,
  seed = 1L
)
}
\arguments{
\item{n}{Number of respondents.}

\item{items}{Number of ordinal indicators per construct.}

\item{loading}{Primary item discrimination proxy used in the data generator.}

\item{missing}{Probability of item-level missingness.}

\item{local_dependence}{Shared residual magnitude for the first two items in
each construct.}

\item{cross_loading}{Loading of the other construct on every item.}

\item{seed}{Integer random seed.}
}
\value{
A data frame with \verb{a*} and \verb{b*} ordinal items and a \code{truth} attribute.
}
\description{
Generates two correlated-free latent constructs with ordinal indicator blocks.
Optional cross-loadings and shared item residuals create controlled measurement
misspecification. The known states are stored in \code{attr(data, "truth")}.
}
\examples{
simulated <- simulate_cssem_data(n = 200, seed = 42)
attr(simulated, "truth")
}
