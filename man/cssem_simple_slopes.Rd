% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moderation.R
\name{cssem_simple_slopes}
\alias{cssem_simple_slopes}
\title{Conditional (simple) slopes of a moderated effect}
\usage{
cssem_simple_slopes(
  association,
  outcome,
  predictor,
  moderator,
  levels = c(-1, 0, 1),
  disattenuate = TRUE,
  eiv_bootstrap = 0L,
  johnson_neyman = TRUE,
  seed = 1L
)
}
\arguments{
\item{association}{A \code{cssem_association} from \code{\link[=cssem_associate]{cssem_associate()}}.}

\item{outcome}{Outcome construct carrying the moderated effect.}

\item{predictor}{Focal predictor whose slope is conditioned.}

\item{moderator}{Moderator construct.}

\item{levels}{Moderator levels in standard-deviation units. Defaults to -1, 0, 1.}

\item{disattenuate}{Disattenuate the focal main effect. Defaults to \code{TRUE}.}

\item{eiv_bootstrap}{Percentile-bootstrap resamples for intervals. Zero omits.}

\item{johnson_neyman}{Whether to locate the Johnson-Neyman region.}

\item{seed}{Bootstrap seed.}
}
\value{
An object of class \code{cssem_simple_slopes}.
}
\description{
Reports the slope of \code{predictor} on \code{outcome} at levels of a \code{moderator}
(\code{slope(w) = b_predictor + b_interaction * w}), with bootstrap intervals and a
Johnson-Neyman region: the range of the moderator over which the conditional
slope is distinguishable from zero. The moderator must appear in a declared
\code{predictor}-by-\code{moderator} interaction on the outcome. The focal main effect is
disattenuated; the interaction term is treated as observed.
}
\examples{
# cssem_simple_slopes(association, "Y", "M", "W", eiv_bootstrap = 500)
}
