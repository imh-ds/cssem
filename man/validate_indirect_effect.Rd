% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation-validation.R
\name{validate_indirect_effect}
\alias{validate_indirect_effect}
\title{Run deterministic mediation validation simulations}
\usage{
validate_indirect_effect(
  manifest,
  reps = 3L,
  seed = 1L,
  folds = 3L,
  iterations = 8L,
  structural_repeats = 3L,
  eiv_bootstrap = 200L,
  workers = 1L
)
}
\arguments{
\item{manifest}{A manifest from \code{\link[=indirect_effect_manifest]{indirect_effect_manifest()}}.}

\item{reps}{Replications per scenario.}

\item{seed}{Base seed.}

\item{folds}{Cross-fitting folds.}

\item{iterations}{Measurement iterations.}

\item{structural_repeats}{Repeated structural CV assignments for selection.}

\item{eiv_bootstrap}{Bootstrap resamples for mediation intervals.}

\item{workers}{Independent jobs to run concurrently.}
}
\value{
A data frame with one row per scenario and replication carrying the
true, naive, and disattenuated indirect effects, absolute biases, and
interval coverage.
}
\description{
Fits CS-SEM, decomposes the mediation effect, and compares the naive and
disattenuated indirect effects against the known latent-scale truth, with
bootstrap-interval coverage. Establishes the workflow that the publication
benchmark (with native PLS-SEM and CB-SEM mediation) will extend.
}
\examples{
results <- validate_indirect_effect(
  indirect_effect_manifest("screening")[1, ], reps = 1, eiv_bootstrap = 50
)
}
