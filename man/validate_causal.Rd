% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-validation.R
\name{validate_causal}
\alias{validate_causal}
\title{Run independent causal-estimand validation simulations}
\usage{
validate_causal(manifest, reps = 3L, seed = 1L, bootstrap = 100L)
}
\arguments{
\item{manifest}{A scenario data frame returned by
\code{\link{causal_validation_manifest}()}.}

\item{reps}{Positive integer replications per manifest row.}

\item{seed}{Integer base seed; each replication receives a deterministic
offset seed.}

\item{bootstrap}{Non-negative integer percentile bootstrap resamples for
linear and mediation intervals. Flexible effect estimands use their
influence-function intervals.}
}
\value{
One row per replication with independent truth, estimate, signed and absolute
bias, interval coverage when available, causal label, graph and adjustment
admissibility, overlap proxy, nuisance diagnostics, runtime, and any failure
text. Unavailable intervals remain NA.
}
\description{
Evaluates causal-effect estimates against fixed analytic or structural
targets, retaining bias, interval coverage when intervals are available,
graph admissibility, an estimand-specific treatment-support proxy, nuisance
diagnostics, failures, and runtime. Runs on locked construct scores: the
measurement-error scenario injects known score error and supplies its known
reliability; it does not refit an item measurement model.
}
\examples{
result <- validate_causal(causal_validation_manifest("screening"),
  reps = 1, bootstrap = 0)
}
