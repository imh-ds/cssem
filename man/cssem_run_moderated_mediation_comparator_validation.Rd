% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moderation-comparator.R
\name{cssem_run_moderated_mediation_comparator_validation}
\alias{cssem_run_moderated_mediation_comparator_validation}
\title{Run the moderated mediation benchmark against native CB-SEM and PLS-SEM}
\usage{
cssem_run_moderated_mediation_comparator_validation(
  manifest,
  reps = 3L,
  seed = 1L,
  folds = 3L,
  iterations = 8L,
  structural_repeats = 3L,
  eiv_bootstrap = 200L,
  seminr_bootstrap = 200L,
  workers = 1L
)
}
\arguments{
\item{manifest}{A manifest from
\code{\link[=cssem_moderated_mediation_validation_manifest]{cssem_moderated_mediation_validation_manifest()}}.}

\item{reps}{Replications per scenario.}

\item{seed}{Base seed.}

\item{folds}{Cross-fitting folds.}

\item{iterations}{Measurement iterations.}

\item{structural_repeats}{Repeated structural CV assignments for selection.}

\item{eiv_bootstrap}{Bootstrap resamples for the CS-SEM index interval.}

\item{seminr_bootstrap}{Bootstrap resamples for the PLS-SEM interval.}

\item{workers}{Independent jobs to run concurrently.}
}
\value{
A data frame with one row per engine, scenario, and replication
carrying the index estimate, absolute bias against truth, interval, and
coverage.
}
\description{
Scores the CS-SEM disattenuated index of moderated mediation against native
PLS-SEM (seminr, composite with a two-stage interaction) and native CB-SEM
(seminr reflective with a product-indicator latent interaction) on a shared
task with a known latent-scale truth, reporting absolute bias and interval
coverage. The CS-SEM naive index is included to show the attenuation the
correction removes. Optional comparators are skipped when their packages are
absent.
}
\examples{
results <- cssem_run_moderated_mediation_comparator_validation(
  cssem_moderated_mediation_validation_manifest("screening")[1, ], reps = 1,
  eiv_bootstrap = 50, seminr_bootstrap = 50
)
unique(results$engine)
}
