% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_states}
\alias{fit_states}
\title{Fit cross-fitted CS-SEM construct states}
\usage{
fit_states(
  model,
  data,
  seed = 1L,
  draws = 0L,
  iterations = 15L,
  tolerance = 0.001,
  quadrature = seq(-4, 4, length.out = 31L),
  diagnostics = TRUE,
  preset = c("default", "exploratory"),
  missing_policy = c("partial", "listwise", "error"),
  split = NULL
)
}
\arguments{
\item{model}{A \code{cssem_model} object.}

\item{data}{A data frame containing all declared indicator columns.}

\item{seed}{Integer random seed for fold assignment and experimental
uncertainty draws.}

\item{draws}{Number of exploratory latent-state uncertainty draws. Set to
zero to omit them. These draws are not release-validated inferential
outputs.}

\item{iterations}{Iteration budget for each measurement fit: the EM loop
stops at convergence or after \code{max(8, 2 * iterations)} iterations. A
warning (class \code{cssem_nonconvergence}) lists any construct whose full-data
or fold encoder stopped at the cap; per-construct status is in
\code{fit$measurement_engine}.}

\item{tolerance}{Positive convergence tolerance for the maximum encoder
parameter change.}

\item{quadrature}{Strictly increasing finite latent-node values used by the
marginal measurement encoder. At least five nodes are required.}

\item{diagnostics}{Whether to calculate exploratory residual diagnostics and
item warnings. Disable for high-throughput simulation benchmarks.}

\item{preset}{Runtime preset. Use \code{"exploratory"} for lighter-weight fitting
defaults while iterating on a live project.}

\item{missing_policy}{How declared indicator missingness is handled. The
default \code{"partial"} retains rows and lets each observed item contribute to
the measurement posterior. \code{"listwise"} excludes rows missing any declared
indicator before fitting. \code{"error"} rejects any missing declared indicator.}

\item{split}{Optional explicit measurement split. Supply a
\code{cssem_splits} object from \code{make_splits()} or an integer fold
assignment with one value per input row. Explicit assignments are retained in
the fitted object's \code{measurement_split} metadata and are filtered
alongside listwise exclusions.}
}
\value{
An object of class \code{fit_states} containing locked scores, full-data
encoders for future scoring, diagnostics, and measurement metadata.
}
\description{
Fits a scale-aware measurement encoder for every theory-declared construct.
Ordinal, continuous, and mixed-item constructs all share one marginal-ML/
EAP measurement model, dispatched per item (graded-response likelihood for
ordinal items, Gaussian likelihood for continuous items) on a common
quadrature grid. A \code{\link[=manifest]{manifest()}} construct instead passes its single
observed column through directly, with no fitted measurement model.
Each returned locked score is predicted by an encoder trained without that
respondent's fold (manifest constructs are standardized out-of-fold too,
from training-fold statistics only).
}
\examples{
data <- simulate_states(n = 80, seed = 1)
model <- cssem_model(list(
  A = list(indicators = paste0("a", 1:4), scales = "ordinal"),
  B = list(indicators = paste0("b", 1:4), scales = "ordinal")
), folds = 3)
fit <- fit_states(model, data, seed = 1, diagnostics = FALSE)
}
\concept{measurement fitting functions}
