% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{manifest}
\alias{manifest}
\title{Declare a manifest (single-item, non-construct) covariate}
\usage{
manifest(indicator, reliability = 1, standardize = TRUE, keys = NULL)
}
\arguments{
\item{indicator}{A single character column name.}

\item{reliability}{Assumed reliability in \verb{(0, 1]}, used by
\code{\link[=associate]{associate()}}'s errors-in-variables correction exactly as a measured
construct's estimated reliability would be. Defaults to \code{1} (treated as
measurement-error-free); supply an externally validated value (e.g.
test-retest reliability) to disattenuate a single-item measure honestly.}

\item{standardize}{Standardize the column to mean 0, SD 1, like every
other locked construct state, so structural coefficients stay
comparable across the model. Set to \code{FALSE} to keep the variable's
natural units.}

\item{keys}{Optional item direction (\code{1} or \code{-1}).}
}
\value{
An indicator specification list, consumed by \code{\link[=specify_measurement]{specify_measurement()}}.
}
\description{
Declares one observed column as a direct structural covariate, bypassing
the measurement encoder entirely: the locked score is the (optionally
standardized) column itself, computed out-of-fold exactly like every
other construct's cross-fitted score, just with nothing estimated to
cross-fit. Use this for controls that are not themselves multi-item
constructs (age, a binary group indicator) or for a deliberately
single-item measure (e.g. a validated single-item life-satisfaction
scale) where an externally known reliability, not an internally
estimated one, is the right correction input.
}
\seealso{
Other model specification functions:
\code{\link[=continuous]{continuous()}},
\code{\link[=cssem_model]{cssem_model()}},
\code{\link[=ordinal]{ordinal()}},
\code{\link[=specify_measurement]{specify_measurement()}},
\code{\link[=specify_structure]{specify_structure()}}
}
\concept{model specification functions}
